<?xml version="1.0"?>
<odoo>

    <record id="view_move_picking_form" model="ir.ui.view">
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_picking_form"/>
        <field name="arch" type="xml">
            <field name="move_dest_ids" position="after">
                <field name="whs_list_ids"/>
            </field>
        </field>
    </record>

    <record id="view_move_form" model="ir.ui.view">
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_form"/>
        <field name="arch" type="xml">
            <field name="move_dest_ids" position="after">
                <field name="whs_list_ids"/>
            </field>
        </field>
    </record>

    <record id="view_picking_form" model="ir.ui.view">
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <button name="button_validate" position="after">
                <button name="button_assign" attrs="{'invisible': ['|', ('state', '!=', 'waiting'), ('show_check_availability', '=', True)]}" string="Mark as Ready" type="object" class="oe_highlight" groups="stock.group_stock_user"/>
            </button>
        </field>
    </record>

</odoo>
