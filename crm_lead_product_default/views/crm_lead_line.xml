<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_tree_lead_line" model="ir.ui.view">
        <field name="name">crm.lead.form.lead</field>
        <field name="model">crm.lead.line</field>
        <field name="inherit_id" ref="crm_lead_product.view_tree_lead_line"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_id']" position="before">
                <field name="sequence" widget="handle"/>
                <field name="product_default_code" invisible="1"/>
                <field name="product_categ_id" invisible="1"/>
                <field name="product_route_ids" invisible="1"/>
            </xpath>
            <field name="category_id" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
            <xpath expr="//field[@name='product_id']" position="attributes">
                <attribute name="context">
                    {'default_type': 'product',
                     'default_default_code': product_default_code,
                     'default_categ_id': product_categ_id,
                     'default_route_ids': product_route_ids}</attribute>
            </xpath>
        </field>
    </record>

</odoo>
